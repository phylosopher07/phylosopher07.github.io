const ICON = document.getElementById("mode_toggle_icon");
function btn_toggle(){
    if (ICON.className == "fas fa-sun" ) {
        ICON.className = "fas fa-moon";
    } else {
        ICON.className = "fas fa-sun";
    }
}
